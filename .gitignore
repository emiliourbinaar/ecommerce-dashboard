# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# Generated data (reproduce with python main.py)
data/raw/
data/processed/
# data/powerbi/ CSVs are tracked so Streamlit Cloud can serve the app without running the pipeline

# Power BI — binary cache files (the PBIP text files and .pbix ARE tracked)

powerbi/**/.pbi/cache.abf
powerbi/**/.pbi/localSettings.json

# OS
.DS_Store
Thumbs.db
